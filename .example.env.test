NODE_ENV=development

# Main API service
MAIN_SERVER_HOST=localhost
MAIN_SERVER_PORT=3000
MAIN_API_START=http://${MAIN_SERVER_HOST}:${MAIN_SERVER_PORT}/api/stream/start
MAIN_API_STOP=http://${MAIN_SERVER_HOST}:${MAIN_SERVER_PORT}/api/stream/stop

# WebSocket service
WEBSOCKET_SERVER_HOST=localhost
WEBSOCKET_SERVER_PORT=3005

# Stream server 
STREAM_SERVER_HOST=localhost
STREAM_PORT=4000

# RTMP server service
RTMP_SERVER_HOST=localhost
RTMP_SERVER_PORT=1935
RTMP_APP=LiveApp
HLS_BROADCAST_PORT=5080
WEB_SERVER_PORT=5443

# MongoDB configuration
MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=
DB_PORT=27017
MONGO_HOST=mongo-db
MONGO_DB=test
DATABASE_URL=mongodb://${MONGO_HOST}:${DB_PORT}/${MONGO_DB}?replicaSet=rs0

# Mongo Express configuration
MONGO_EXPRESS_PORT=8081
ME_CONFIG_MONGODB_ADMINUSERNAME=
ME_CONFIG_MONGODB_ADMINPASSWORD=
ME_CONFIG_MONGODB_URL=${DATABASE_URL}
ME_CONFIG_BASICAUTH=false
ME_CONFIG_MONGODB_REPLICA_SET=rs0
